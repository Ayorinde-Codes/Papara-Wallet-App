# Papara Wallet App

Papara is a wallet app built in Vue 3.

## 🚀 Features

- **Vue 3**
- **Tailwind CSS** for all styling 
- **PWA Support** - Installable on mobile devices
- **Dark/Light Mode** toggle with system preference detection
- **Mobile-First Responsive** design
- **Vue Router 4**
- **Lucide Icons**

## 📦 Prerequisites

Before running this project, make sure you have:

- **Node.js** version 22 or higher
- **npm** version 10 or higher

You can check your versions with:
```bash
node --version  # Should be v22.x.x or higher
npm --version   # Should be 10.x.x or higher
```

## 🛠️ Installation

1. **Clone the repository**
```bash
git clone <repository-url>
cd Papara-Wallet-App
```

2. **Install dependencies**
```bash
npm install
```

3. **Start the development server**
```bash
npm run dev
```

4. **Open your browser**
Open the URL shown in the terminal (usually `http://localhost:5173`)

## 📋 Available Scripts

| Command | Description |
|---------|-------------|
| `npm run dev` | Start development server |
| `npm run build` | Build for production |
| `npm run preview` | Preview production build locally |

## 📱 PWA Installation

To test PWA features, build and preview:
```bash
npm run build
npm run preview
```
Open the URL shown in the terminal, then install:
- **Desktop (Chrome)**: Install icon in address bar
- **Mobile**: Share → Add to Home Screen

## 🎨 Typography

- Font: **DM Sans** (Google Fonts)
- Fallback: SF Pro Display, system fonts

## 📱 Screens Implemented

| Screen | Description |
|--------|-------------|
| **Home** | Main dashboard with balance carousel, stories, transactions, cashback |
| **QR Operations** | QR code payment and ATM operations |
| **Transfer** | Money transfer options |
| **Payments** | Bill payments and services |
| **Cards** | Card showcase (Gray, Black, EDU cards) |
| **Stats** | Spending statistics with donut chart |


